# 歌单订阅（MoviePilot V3）

## 上游引用

| 项 | 内容 |
| --- | --- |
| 原始上游 | https://github.com/baozaodetudou/MoviePilot-Plugins（作者：逗猫） |
| 上游基线 | v7.2（V2 实现，未随本仓库发布） |
| 本仓库演化 | 上游 v7.2 → 本地 V2 7.2（网易云登录增强）→ 本地 V3 8.0.0（改用本地 ncm-api） |
| 引入时间 | 2026-09 重建本仓库时确认 |

> V3 版本为本仓库自有适配实现：网易云登录与取数改走本地部署的 `moefurina/ncm-api`，
> 与上游 V2 实现的直连方式不同，详见下文。

---

把 QQ 音乐 / 网易云音乐 / 汽水音乐的歌单同步到 Plex、Emby 的音乐库播放列表。

V3 版本与旧版最大的区别：**网易云的登录与取数不再由插件自己直连网易官方接口**，
而是统一交给本地部署的 [`moefurina/ncm-api`](https://hub.docker.com/r/moefurina/ncm-api) 容器。
插件因此去掉了内置的 1.3MB `NeteaseCloudMusicApi.js` 和 `py_mini_racer`（V8 引擎）两个依赖，
加密、风控、解灰适配由 ncm-api 维护。

## 1. 部署 ncm-api

插件本身不包含 ncm-api，需要单独起一个容器。
**ncm-api 的默认容器端口 3000 与 MoviePilot 冲突，宿主机端口统一映射为 1630**，
插件默认地址也按此填写。三种常见部署方式，配置页里填对应地址即可。

### 方式一：与 MoviePilot 同一个 docker-compose（推荐）

```yaml
services:
  moviepilot:
    image: jxxghp/moviepilot-v2:latest
    # ... 已有配置 ...
    networks:
      - moviepilot

  ncm-api:
    image: moefurina/ncm-api:latest
    container_name: ncm-api
    restart: unless-stopped
    ports:
      - "1630:3000"
    networks:
      - moviepilot

networks:
  moviepilot:
```

同一网络下用容器名互访，插件里填 `http://ncm-api:1630`（容器内仍是 3000，映射后宿主机是 1630，
插件与 ncm-api 同网络时填 `http://ncm-api:3000` 也可以，走的是容器内部端口）。

### 方式二：ncm-api 跑在宿主机

```bash
docker run -d --name ncm-api -p 1630:3000 --restart unless-stopped moefurina/ncm-api:latest
```

MoviePilot 在容器里，访问宿主机端口用 `http://host.docker.internal:1630`
（Linux 下需要给 MoviePilot 容器加 `--add-host=host.docker.internal:host-gateway`）。

### 方式三：ncm-api 在局域网另一台机器

填 `http://192.168.x.x:1630`。

> 部署完可以先在浏览器打开 `http://<地址>/inner/version`，
> 能返回 JSON 就说明服务正常；插件的配置页也有「测试连接」按钮做同样的探测。

## 2. 配置项

### ncm-api 服务地址

插件默认值 `http://192.168.1.100:1630` **只是占位**，必须改成你实际的地址，否则网易云相关功能不可用。
地址可以省略 `http://` 前缀，插件会自动补，末尾多余的 `/` 也会去掉。
填完可以点旁边的「测试连接」按钮，不用保存配置就能验证地址是否可用。

### 防风控

插件内置了两项防风控措施：所有请求自动带时间戳参数避开 ncm-api 的 2 分钟缓存，
相邻请求之间强制 0.3~0.8 秒随机间隔，避免触发网易 IP 高频限制；
登录状态还在时不会重复调登录接口。

### 网易云登录方式

四种方式，**配置页里选哪种，就只显示哪种的栏位和按钮**：

| 方式 | 说明 |
| --- | --- |
| **扫码登录（推荐）** | 点「获取二维码」二维码直接显示在配置页 → 用网易云音乐 App 扫码 → 点「检查扫码结果」完成登录。最稳，不受密码风控影响 |
| 短信验证码 | 填手机号 → 点「发送验证码」→ 填验证码 → 点「验证码登录」 |
| 手机号 / 邮箱 + 密码 | 网易网易云盾对密码登录风控最严，容易返回 `502` 或触发验证，仅在扫码/验证码都不可用时使用 |
| 手动粘贴 Cookie | 自己在浏览器登录网易云，复制包含 `MUSIC_U` 的完整 Cookie 填进来 → 点「校验并保存 Cookie」 |

所有按钮都是即时生效（不用先保存配置），登录成功后 Cookie 立即落盘到插件数据目录（`cookie_storage`）。
**不要频繁调登录接口**——登录状态还在时插件不会重复登录。

### Cookie 保活

默认开启：每天 9 点自动检查一次登录状态并尝试刷新（调用 `/login/refresh` 续期 Cookie）。
注意 ncm-api 的刷新接口**不支持二维码登录得到的 Cookie**，扫码账号只做状态确认；
Cookie 失效时，密码登录方式会用保存的账号密码自动重登，其他方式会在日志里提示重新登录。

### 歌单同步配置（QQ音乐 / 网易云 / 汽水音乐，三处互相独立）

每行一条，格式为 `歌单id:播放列表名称`，冒号是英文冒号。
**三行配置不会互相影响**：某一行写错只会跳过该行并记一条异常，不再中断整轮同步。

```text
# ① QQ音乐歌单同步设置
2362260213:经典歌曲
2362260213:经典歌曲:emby用户名

# ② 网易云歌单同步设置（只填网易云）
365436873:我的歌单
365436873:我的歌单:emby用户名
365436873:我的歌单:emby1,emby2

# ③ 汽水音乐歌单同步设置（只填汽水分享链接）
https://qishui.douyin.com/xxxx:华语精选
https://qishui.douyin.com/xxxx:华语精选:emby用户名
```

> QQ 音乐歌单 id 取自歌单链接 `https://y.qq.com/n/ryqq/playlist/歌单id`；
> 网易云歌单 id 取自 `https://music.163.com/#/playlist?id=歌单id`。
> 汽水音乐现在有独立栏位；旧配置把汽水链接写在网易云栏位里仍然兼容，但会在日志里提示迁移。

#### 汽水音乐分享链接怎么获取

汽水音乐没有自部署 API，插件走 [PlaylistOut](https://github.com/LengxiQwQ/music-playlist-exporter)
公共解析服务取歌（限流 30 次/分钟，**无需任何账号或凭证**），所以只要拿到一条歌单分享链接就行：

| 步骤 | 操作 |
| --- | --- |
| 1 | 打开汽水音乐 App，底部进入「我的」 |
| 2 | 点开「我创建的歌单」或「我收藏的歌单」里的目标歌单 |
| 3 | 歌单详情页**右上角三个点 `⋮`** → 选「分享」 |
| 4 | 在弹出面板里选**「复制链接」**（不要选分享到微信，那会变成小程序卡片） |
| 5 | 回到插件配置页，粘贴到「汽水音乐歌单同步设置」，后面补 `:播放列表名称` |

复制出来的链接形如 `https://qishui.douyin.com/xxxxxxx` 或
`https://music.douyin.com/qishui/share/playlist?playlist_id=xxxx`，两种都能识别。

注意事项：

- **歌单必须能公开访问**。私密歌单在 App 里也能生成链接，但解析服务拿不到曲目，会报「歌单不存在」。
- 复制链接时如果只复制到「口令 + 链接」的整段文本，插件会从中自动提取 URL，多出来的文字会被忽略。
- 一行只放一个歌单；`播放列表名称` 留空时用汽水歌单原名。
- 解析服务是第三方公共接口，偶发限流（30 次/分钟）时会提示「请稍后再试」，下一轮同步会自愈。

### 每日推荐（歌单 / 歌曲）

勾选「同步每日推荐歌单」或「同步每日推荐歌曲」后，同步时会额外拉取网易云的每日推荐：

- 每日推荐歌单：取推荐列表里的歌单（默认前 5 个）逐个同步；
- 每日推荐歌曲：同步到一个名为「每日歌曲推荐」的播放列表。

**这两个接口都必须处于登录状态**，且是网易风控最严的接口。失败时插件会给出明确原因，
不再静默跳过：

| 日志/详情页提示 | 原因与处理 |
| --- | --- |
| `code=301，需要登录（Cookie 已失效或未登录）` | 到插件配置页重新扫码登录 |
| `返回的推荐列表为空` / `dailySongs 为空` | 未登录、Cookie 失效，或当日推荐尚未生成 |
| `code=460，触发网易风控（cheating）` | 降低同步频率，或让 ncm-api 换国内出口 IP |
| 「网易云未登录或 Cookie 已失效」 | 同步时会实时复查登录态，重新登录后即可恢复 |

配置页有「**诊断每日推荐接口**」按钮，点一下直接显示 ncm-api 版本、当前账号，
以及两个接口各自的 `code` 与返回条数，便于定位是登录问题还是数据问题。

### 插件详情页

点插件卡片的「详情」可以看到：

- **同步情况**：上次同步时间与耗时、媒体服务器、每日推荐开关、网易云账号；
- **同步明细**：每个歌单的来源（QQ音乐 / 网易云 / 汽水音乐 / 每日推荐）、目标播放列表、
  歌曲总数、已存在、本次新增、库内缺失、状态与失败原因；
- **订阅情况**：本次新增 / 已存在或未识别 / 失败的数量，以及本插件累计登记的订阅数量、
  宿主音乐类订阅总数；
- **音乐订阅明细**：订阅 ID、歌曲、歌手、实时订阅状态、媒体标识、创建时间。

### 音乐订阅管理

删除订阅在**配置页 → 音乐订阅管理**（保存配置后立即执行，不可撤销）：

| 配置项 | 作用 |
| --- | --- |
| 删除本插件创建的全部音乐订阅 | 只删插件同步时登记过的订阅，不影响你手动订阅的音乐 |
| 删除身份不完整的音乐订阅 | 清掉 `media_source`/`media_id` 为空的识别残留脏数据 |
| 按订阅 ID 删除 | 填 ID（逗号分隔），执行后输入框自动清空；ID 见详情页表格 |

也可以直接调接口（需带 MoviePilot 的 Bearer Token）：

```bash
# 查看本插件登记的订阅
GET  /api/v1/plugin/MusicSubscribe/subscribes
# 按 ID 删除
POST /api/v1/plugin/MusicSubscribe/subscribes/remove?ids=12,15
# 批量清理：scope=own（本插件创建）/ scope=unrecognized（身份不完整的脏数据）
POST /api/v1/plugin/MusicSubscribe/subscribes/clear?scope=own
# 查询最近一次同步统计
GET  /api/v1/plugin/MusicSubscribe/stats
# 诊断每日推荐接口
GET  /api/v1/plugin/MusicSubscribe/diagnose
```

> 为什么不在详情页直接放删除按钮：MoviePilot 的 Vuetify JSON 模式下**只有配置页支持
> `{{ }}` 表达式与 `onxxx` 事件**，详情页放按钮会变成点不动的假交互，所以统一收敛到配置页。

### 音乐订阅（库内没有的歌曲）

开启「库内没有的歌曲转为音乐订阅」后，同步时在 Plex/Emby 里没搜到的歌曲
会自动添加为 MoviePilot 音乐订阅（单曲，recording），由宿主的订阅链
（MusicBrainz 等识别源）负责后续识别与下载；识别失败的歌曲会记日志跳过，
不影响其余歌曲同步。

### 其他

- **精准匹配**：开启后要求歌手也匹配，避免翻唱误入库。歌单里有冷门版本时可以先关掉排查。
- **立即运行一次**：保存配置后立刻跑一次同步，方便验证。

## 3. 使用前提

1. 本地已部署并可访问 ncm-api，且网易云已登录。
2. Plex / Emby 中已存在**音乐类型**的媒体库。
3. 目标播放列表建议提前手动创建并放入至少一首歌（Plex 会自动创建，Emby 需要已有）。
4. Plex 使用最高码率版本入库；同一首歌在库里有多个版本时会自动取码率最高的那条。

## 4. 升级说明（v2 → v3）

| 项目 | v2 | v3 |
| --- | --- | --- |
| 目录 | `plugins.v2/syncmusiclist/` | `plugins.v3/musicsubscribe/` |
| 索引 | `package.v2.json` | `package.v3.json` |
| 宿主接口 | `app.core` / `app.helper` | `app.sdk` |
| 网易云链路 | 内置 JS + py_mini_racer 直连网易 | 本地 ncm-api HTTP |
| 额外依赖 | `py_mini_racer`、`requests` | 无（全部走宿主已提供的依赖） |
| Plex 检索 | 手工拼 `/hubs/search` | 媒体库级 `searchTracks()`（与宿主 `Plex.get_music()` 同链路） |
| 版本 | 7.2 | 8.0.0 |
| 版本（当前） | — | 8.3.0 |

版本与目录名（musicsubscribe）按代际跃迁规则从 `7.2` 跳到 `8.0.0`，配置项与 v2 保持兼容，
已配置的网易云账号密码、歌单同步列表升级后可直接沿用。

### 8.2.0 → 8.3.0 变更

| 项目 | 说明 |
| --- | --- |
| 配置拆分 | 新增「汽水音乐歌单同步设置」，与「网易云歌单同步设置」分开；旧的混填方式仍兼容并提示迁移 |
| 每日推荐修复 | 校验 ncm-api 返回码并把原因写进日志；同步时实时复查登录态，不再依赖 init_plugin 时的缓存；新增「诊断每日推荐接口」按钮与 `/diagnose` 接口 |
| 容错修复 | 单行配置格式不规范由 `return` 改为 `continue`，不再中断整轮同步（原行为会让后续所有歌单与每日推荐全部不执行） |
| 详情页 | 新增 `get_page()`：同步情况、同步明细、订阅数量与订阅明细表 |
| 订阅管理 | 新增「按 ID 删除 / 清理本插件订阅 / 清理身份不完整订阅」三个动作与 4 个管理接口 |

## 5. 故障排查

| 现象 | 排查方向 |
| --- | --- |
| 保存配置报「服务地址不可达」 | 核对地址与端口；MoviePilot 容器内 `curl http://<地址>/inner/version` 是否通 |
| 提示 `460 cheating` | ncm-api 出口 IP 被网易限制，更换 ncm-api 部署网络（国内家宽/代理出口） |
| 提示 `502` | 多出现在密码登录，换扫码或 Cookie 登录 |
| 歌单拉取返回 403 / 空 | 网易云 Cookie 失效，重新扫码；ncm-api 需要更新到最新镜像 |
| 每日推荐不同步 | 先点「诊断每日推荐接口」：未登录（code=301）→ 重新登录；返回 0 条 → 当日推荐为空或 Cookie 失效；code=460 → 风控 |
| 某一行配置没生效 | 日志会写「配置格式不规范，已跳过该行」，检查冒号是否为英文、字段数是否为 2 或 3 |
| 汽水歌单报「歌单不存在」 | 歌单未公开；或复制的是分享口令而非链接，重新在歌单页 `⋮ → 分享 → 复制链接` |
| 汽水歌单报「解析服务限流」 | 公共解析服务 30 次/分钟，降低同步频率后重试 |
| Plex 搜不到歌曲 | 确认媒体库类型是「音乐」；歌名或歌手在库里不一致时先关掉精准匹配试一次 |
| 播放列表没变化 | 打开「详情」看同步明细的「已存在 / 本次新增 / 库内缺失」，确认不是已经同步过 |
| 音乐订阅太多想清理 | 配置页「音乐订阅管理」：删本插件创建的、删身份不完整的，或按详情页的 ID 精确删除 |
