# 歌单订阅（MoviePilot V3）

把网易云 / QQ音乐 / 汽水音乐的歌单同步成 Plex / Emby 的播放列表，并把媒体库里搜不到的曲目汇总成一份**待订阅清单**，由你在数据页勾选后一键推送订阅。

**只做两件事：**

1. **同步 / 创建歌单** —— 网易云、QQ音乐、汽水音乐的歌单，以及网易云每日推荐（歌单 / 歌曲），同步成 Plex / Emby 的音乐播放列表，曲库里已有的曲目不重复添加；
2. **库内缺失歌曲订阅** —— 同步时媒体库里搜不到的曲目，按「歌手 + 歌名」跨歌单去重后落到清单里（含歌曲名、时长、歌手、专辑名称、来源歌单、命中次数），在数据页逐行勾选「歌曲」或「专辑」，点「订阅」推送给 MoviePilot 的音乐订阅。

插件**不会**在同步过程中自动订阅任何东西，每首歌怎么订阅都由你决定。

---

## 1. 部署 ncm-api（网易云取数的前置条件）

网易云的登录与取数全部交给本地部署的 [ncm-api](https://github.com/moefurina/ncm-api) 容器，插件不内置加密实现。
ncm-api 容器端口 3000 与 MoviePilot 冲突，宿主机映射端口统一用 **1630**。

**与 MoviePilot 同一个 docker-compose（推荐）**

```yaml
services:
  moviepilot:
    image: jxxghp/moviepilot:latest
    # ……省略自身配置……
  ncm-api:
    image: moefurina/ncm-api:latest
    container_name: ncm-api
    restart: always
    ports:
      - "1630:3000"
```

服务地址填 `http://192.168.X.X:1630`（同一 compose 网络内可写 `http://ncm-api:3000`）。

**ncm-api 跑在宿主机**

```bash
docker run -d --name ncm-api -p 1630:3000 moefurina/ncm-api:latest
```

服务地址填 `http://127.0.0.1:1630`（MoviePilot 也跑在宿主机时）或宿主机内网 IP。

**在局域网另一台机器**：服务地址填 `http://那台机器的内网IP:1630`，注意那台机器要能访问网易云。

---

## 2. 配置页

| 分区 | 说明 |
| --- | --- |
| 运行设置 | 启用开关、定时同步周期（cron）、同步到哪些媒体服务器、曲目精确匹配、保存后立即运行一次 |
| 网易云 | ncm-api 服务地址 + 连通性探测、四种登录方式、当前登录状态与「退出登录」按钮 |
| 歌单来源 | 网易云歌单 / 每日推荐、QQ音乐歌单、汽水音乐歌单 |
| 上次同步 | 上次同步耗时、新增曲目、库内缺失数量，以及每个播放列表的结果明细 |

### 2.1 网易云登录

支持四种方式。**退出登录是一次性按钮动作**，插件不再有「Cookie 保活」这类后台开关：

- **扫码登录**（推荐）：点「获取二维码」→ 用网易云音乐 App 扫码 → 页面自动轮询到授权成功；
- **手机验证码**：填手机号 → 点「发送验证码」→ 填验证码登录；
- **账号密码**：手机号或邮箱 + 密码，凭据随配置保存，Cookie 失效时可自动续登；
- **手动粘贴 Cookie**：粘贴包含 `MUSIC_U` 的完整 Cookie。

### 2.2 歌单配置格式

每行一条，空行与 `#` 开头的注释行会被忽略：

```
# 网易云 / QQ音乐：歌单ID:播放列表名称[:emby用户名]
2388086885:我喜欢的音乐
https://music.163.com/playlist?id=2388086885:午后咖啡
2388086885:收藏:张三,李四

# 汽水音乐：分享链接:播放列表名称[:emby用户名]
https://.../xxx:华语精选
```

- `播放列表名称` 是同步后在 Plex / Emby 里显示的歌单名，必填；
- `emby用户名` 只对 Emby 有效（多人各用各的播放列表），多个用户用半角逗号分隔；
- 网易云歌单可以直接粘贴歌单链接，插件会自己从中取出数字 ID；
- 汽水音乐填汽水 App 里复制的歌单分享链接；历史配置里把汽水链接写在「网易云」栏位也能识别（会提示建议迁移）。

### 2.3 曲目精确匹配

- **开启**（默认）：按「歌名 + 歌手」精确判断曲库里是否已有这首；
- **关闭**：只按歌名模糊匹配，匹配更宽松，但可能并入同名不同版本的曲目。

---

## 3. 数据页（库内缺失曲目）

列表与你看到的截图完全一致：

| 序号 | 选择歌曲 | 选择专辑 | 歌曲名称 | 时长 | 歌手 | 专辑名称 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | ○ | ● | 我记得 | 05:29 | 赵雷 | 署前街少年 |
| 2 | ● | ○ | 红豆 | 04:16 | 王菲 | 畅游 |

- **同一序号的「歌曲」与「专辑」二选一**：同一个 `seq` 只能是一种粒度，点另一个会自动切过来；
- **全选**：把当前筛选出来的记录全部选中为「歌曲」；
- **反选**：未勾选 → 勾选歌曲；已选歌曲 → 切成专辑（没有专辑信息则取消）；已选专辑 → 取消；
- **订阅**：按勾选的粒度逐条推送给 MoviePilot 音乐订阅，结果回写到清单（成功 / 失败原因、订阅类型、时间）；
- **移除 / 清理**：移除勾选的记录，或清理「已订阅」/「全部」；
- 顶部统计卡可切换「全部 / 待订阅 / 已订阅」，另有关键词搜索、数据源过滤与分页（每页 20 / 50 / 100 条）。

清单上限 1000 条，超出后丢弃最早的记录。

**执行顺序**：一次同步里先把所有歌单推完（该加的歌照常加进播放列表），每个歌单里搜不到的歌只做登记并按「歌名 + 歌手」跨歌单去重（命中次数累加、来源保留第一次见到的歌单），全部推完后统一写入清单。中途出错也不丢已发现的缺歌。

---

## 4. 订阅是怎么落的

宿主按 `meta` 识别音乐时只走主来源 MusicBrainz，中文曲库命中率很低，常表现为「未识别到媒体信息」。
这里改用宿主内置的**豆瓣音乐源**识别，识别结果自带媒体身份，再带 `media_source` / `media_id` 显式订阅：

- 选「歌曲」→ 按单曲（`recording`）订阅；
- 选「专辑」→ 按所在专辑（`album`）订阅。宿主要求专辑识别结果带曲目总数，拿不到就按失败处理并在清单里写明原因；
- 只采信**音乐来源**的识别结果。宿主的识别是插件优先的模块广播，影视类插件若不校验 `media_source` 就按标题返回结果会抢先命中，这类结果一律按未识别处理；
- 同步过程中**不再**做宿主 MusicBrainz 兜底识别（旧版开关已移除），每首缺歌一次豆瓣识别就结束，不会连带惊动影视类插件。

豆瓣也认不出来的曲目，清单里会写明「豆瓣音乐源未识别到歌曲身份」，留在清单里等你自己决定要不要用别的方式找。

---

## 5. 定时任务

在「运行设置」填五位 cron 表达式即可，例如每天凌晨 4 点：`0 4 * * *`。
留空表示不定时运行，只能手动点「立即运行一次」。「保存后立即运行一次」是一次性开关：保存后跑一轮，随后自动复位。

---

## 6. 接口

完整路径为 `/api/v1/plugin/MusicSubscribe/<path>`：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/status` | 版本、开关、登录态、媒体服务器、清单概览、上次同步统计 |
| GET | `/probe` | 探测 ncm-api 连通性 |
| POST | `/qrcode` | 获取扫码登录二维码 |
| GET | `/qrcode/status` | 查询扫码状态（成功后自动保存登录） |
| POST | `/captcha/send` | 发送登录短信验证码 |
| POST | `/login` | 验证码 / 密码 / Cookie 登录 |
| POST | `/logout` | 退出网易云登录并清除本地凭证 |
| POST | `/run` | 立即运行一次同步 |
| GET | `/pending` | 查询库内缺失曲目清单（`scope=all/pending/subscribed`） |
| POST | `/pending/subscribe` | 按勾选的粒度推送订阅（body：`{"items":[{"seq":1,"target":"song"}]}`） |
| POST | `/pending/remove` | 从清单移除勾选的记录 |
| POST | `/pending/clear` | 清理清单（`scope=all/subscribed`） |

---

## 7. 前端开发

```bash
cd frontend
npm install
npm run build    # 产出 frontend/dist/assets/remoteEntry.js（宿主按联邦方式加载 ./Config 与 ./Page）
npm run dev      # 本地预览 http://localhost:5101（自带 mock 接口）
```

Vuetify 由宿主提供，构建时会剥掉产物里的 `.v-*` / `.mdi-*` 规则，避免覆盖宿主样式。
详见 [`frontend/README.md`](frontend/README.md)。
